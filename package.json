{
  "name": "oidc-aws-github",
  "version": "0.1.0",
  "bin": {
    "oidc-aws-github": "bin/oidc-aws-github.js"
  },
  "scripts": {
    "build": "tsc",
    "ci": "npm run lint && npm run build && npm run test && cdk synth --quiet",
    "lint": "eslint",
    "test": "jest",
    "test:update": "jest -u"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.154.1",
    "constructs": "^10.0.0"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^5.4.1",
    "@types/jest": "^29.5.12",
    "@types/node": "^22.5.0",
    "aws-cdk": "^2.154.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.2"
  }
}
